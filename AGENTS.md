# AGENTS.md

- Coding convention: see `context/zw-js-coding-preference.md`.
- Commit convention: see `context/git-commit-convention.md`.
- Stack: vanilla HTML, CSS, and JavaScript; no frameworks, build step, or dependencies.
- Structure: root `index.html`; `views/` for secondary HTML views; `js/` for JavaScript; `styles/` for CSS.
- Testing: keep probability math pure and testable with Node.
- Deployment: GitHub Actions deploys to GitHub Pages on pushes to `main`.
